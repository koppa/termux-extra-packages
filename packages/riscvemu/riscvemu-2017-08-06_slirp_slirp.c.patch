diff -uNr riscvemu-2017-08-06/slirp/slirp.c riscvemu-2017-08-06.mod/slirp/slirp.c
--- riscvemu-2017-08-06/slirp/slirp.c	2017-08-06 15:30:12.000000000 +0300
+++ riscvemu-2017-08-06.mod/slirp/slirp.c	2018-06-27 23:04:53.848753567 +0300
@@ -114,7 +114,7 @@
             return 0;
         }
         old_stat = dns_addr_stat;
-        if (stat("/etc/resolv.conf", &dns_addr_stat) != 0)
+        if (stat("@TERMUX_PREFIX@/etc/resolv.conf", &dns_addr_stat) != 0)
             return -1;
         if ((dns_addr_stat.st_dev == old_stat.st_dev)
             && (dns_addr_stat.st_ino == old_stat.st_ino)
@@ -125,7 +125,7 @@
         }
     }
 
-    f = fopen("/etc/resolv.conf", "r");
+    f = fopen("@TERMUX_PREFIX@/etc/resolv.conf", "r");
     if (!f)
         return -1;
 
